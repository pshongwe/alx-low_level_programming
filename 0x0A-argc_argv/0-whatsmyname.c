#include <stdio.h>

/**
 * main - entry point
 * @argc: length of arg array
 * @argv: arg array
 *Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
printf("%s\n %d", argv[0], argc);
return (0);
}

